{
    "modelFile": "/data/objectbox-model.json",

    "_dbMaxSize note": "Note: 1G is more than plenty for this example",
    "dbMaxSize": "1G",

    "_bind note": "Note: need to bind to 0.0.0.0 so we can publish the port in Docker:",
    "bind": "ws://0.0.0.0:9999",
    "adminBind": "http://0.0.0.0:9980",

    "_unsecuredNoAuthentication note": "Do not enable unsecuredNoAuthentication for production:",
    "unsecuredNoAuthentication": true,

    "_debug note": "Remove the underscore in the next line to enable debug logging",
    "_debug": true
}